import 'package:senior_code_app/core/extensions.dart';
import '../../../../exports.dart';
import 'change_password_app_bar_section.dart';
import 'channge_password_form_section.dart';

class ChangePasswordScreenBody extends StatelessWidget {
  const ChangePasswordScreenBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        42.vs,
        const ChangePasswordAppBarSection(),
        28.vs,
        const ChanngePasswordFormSection(),
      ],
    );
  }
}
